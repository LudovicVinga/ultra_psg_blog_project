{% extends 'themes/base_user.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block description %}Consultez et gérez les informations de votre profil personnel, y compris vos coordonnées et les options de sécurité. Modifiez facilement votre profil pour garder votre compte à jour.{% endblock %}

{% block keywords %}profil utilisateur, informations personnelles, compte, gestion du profil, paramètres du compte, sécurité, modifier profil, coordonnées, données utilisateur, espace personnel, mise à jour profil, profil en ligne, confidentialité{% endblock %}

{% block main %}
    
    <div class="text-center my-3">
        <h1 class="text-danger display-5 fw-bold fst-italic">MON PROFIL</h1>
        <h4 class="text-primary">Consultez et modifiez vos informations personnelles</h4>
    </div>


    <div class="container mt-5">
        <div class="row">
            {# image #}
            <div class="col-lg-6 text-center d-flex flex-column justify-content-center align-items-center">
                <img src="/images/static/user/parc.png" class="img-fluid border border-5 border-primary shadow" alt="Vu des tribunes du parc des princes">
            </div>

            {# Info utilisateurs #}
            <div class="col-lg-6 d-flex flex-column justify-content-center align-items-center py-3">

                {% include 'components/alert.html.twig' with {"addMarginTop": "mt-3"} %}

                <div>
                    <p>
                        <strong class="text-danger">Prénom</strong> : {{app.user.firstName}}
                    </p>
                    <p>
                        <strong class="text-danger">Nom</strong> : {{app.user.lastName}}
                    </p>
                    <p>
                        <strong class="text-danger">Email</strong> : {{app.user.email}}
                    </p>
                    <div>
                        {# Dans ce cas précis, on sait que la personne qui cherche a modifier le profil est la personne connectée(ADMIN). Donc pas besoin de spécifier l'id en 2nd paramètre #}
                        <a href="{{path('app_user_profile_edit_profile')}}" class="m-2 btn btn-sm btn-primary">Modifier mon profil</a>
                        <a href="{{path('app_user_profile_edit_password')}}" class="m-2 btn btn-sm btn-dark">Modifier mon mot de passe</a>
                    </div>
                </div>
            </div>








            {# <div class="col-md-8 mx-auto shadow p-4 text-center">

                {% include 'components/alert.html.twig' with {"addMarginTop": "mt-3"} %}

                <div>
                    <p>
                        <strong>Prénom</strong>: {{app.user.firstName}}
                    </p>
                    <p>
                        <strong>Nom</strong>: {{app.user.lastName}}
                    </p>
                    <p>
                        <strong>Email</strong>: {{app.user.email}}
                    </p>
                    <div>
                        Dans ce cas précis, on sait que la personne qui cherche a modifier le profil est la personne connectée(ADMIN). Donc pas besoin de spécifier l'id en 2nd paramètre
                        <a href="{{path('app_user_profile_edit_profile')}}" class="m-2 btn btn-sm btn-primary">Modifier mon profil</a>
                        <a href="{{path('app_user_profile_edit_password')}}" class="m-2 btn btn-sm btn-dark">Modifier mon mot de passe</a>
                    </div>
                </div>
            </div> #}
        </div>
    </div>
{% endblock %}