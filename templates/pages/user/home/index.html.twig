{% extends "themes/base_user.html.twig" %}

{% block title %}Accueil{% endblock %}

{% block description %}Découvrez votre espace utilisateur personnalisé : gérez facilement vos informations, accédez à vos services et profitez d’une expérience sur mesure.{% endblock %}

{% block keywords %}espace utilisateur, compte utilisateur, tableau de bord, gestion compte, profil personnel, paramètres utilisateur, informations personnelles, connexion compte{% endblock %}

{% block main %}

    {% include 'components/welcomeTitle.twig' %}

    <div class="container my-5 text-center">
        <div class="row">
            <div class="col-md-6">
                <div class="card border-primary mb-3">
                    <div class="card-header bg-transparent border-primary">Tous vos commentaires</div>
                    <div class="card-body">
                        <p class="card-text">{{ app.user.comments|length }}</p>
                    </div>
                    <div class="card-footer bg-transparent border-danger">
                        <a href="{{path('app_visitor_news')}}" class="btn btn-primary">Commenter les news</a>
                    </div>
                </div>
            </div>

            <div class="col-md-6">
                <div class="card border-primary mb-3">
                    <div class="card-header bg-transparent border-primary">Tous vos likes</div>
                    <div class="card-body">
                        <p class="card-text">{{ app.user.likes|length }}</p>
                    </div>
                    <div class="card-footer bg-transparent border-danger">
                        <a href="{{path('app_visitor_news')}}" class="btn btn-primary">Liker les news</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}